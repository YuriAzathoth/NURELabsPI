SET (TARGET_NAME nanodbc)
ADD_LIBRARY (${TARGET_NAME} STATIC nanodbc.cpp nanodbc.h)
TARGET_INCLUDE_DIRECTORIES (${TARGET_NAME} PRIVATE ${CMAKE_SOURCE_DIR} ${ODBC_INCLUDE_DIRS})
TARGET_LINK_LIBRARIES (${TARGET_NAME} PRIVATE ${ODBC_LIBRARIES})
